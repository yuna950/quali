# QUALI 진행 상황 (2026-09-11 기준)

여러 컴퓨터(집/학원)를 오가며 작업 중이라 만든 인수인계용 문서.
새 컴퓨터에서 이어갈 때는 `git pull` 후 이 파일부터 읽으면 됨.

## 서비스 개요

자격증 정보 + 나의 시험 일정/응시 기록을 한 곳에서 관리하는 개인 맞춤형 자격증 관리 서비스.
Q-net 공공데이터 API(XML)를 Supabase Edge Function이 받아 테이블로 적재하고, 프론트는 Supabase만
조회하는 구조로 갈 계획이지만, **지금은 프론트엔드를 mock 데이터로 전부 완성하는 단계**이고
백엔드는 아직 손대지 않았음.

## 작업 순서 (합의된 원칙)

데이터 모델 → 공통 컴포넌트 → 메인페이지 → 검색페이지 → 상세페이지 → 마이페이지 → 인증 화면
→ (전부 끝난 뒤) Supabase/Edge Function 연동

## Phase별 진행 상황

- **Phase 1 (데이터 모델/공통 세팅)**: 완료. shadcn/ui(Base UI + Tailwind v4), 타입 정의(`src/types`),
  mock 데이터(`src/mocks`), 데이터 접근 레이어(`src/services`), 테스트 계정 인증(`src/lib/auth.tsx`)
- **Phase 2 (공통 컴포넌트)**: 완료. `CertificateCard`, `StatusBadge`, `DdayBadge`, `InterestButton`
- **Phase 3 (메인페이지)**: 완료. Hero 캐러셀, 관심 직무분야, 주간 시험일정 캘린더
- **Phase 4 (검색페이지)**: 완료. 직무분야→분류→시행종목 3단 계층 브라우저 + 상태 필터
- **Phase 5 (자격증 상세페이지)**: 완료. 응시료/과목/시험일정(회차별)/합격률(5년)/유사자격증
  - 이번에 "관련 응시기록" 섹션을 추가했다가 **다시 제거하기로 결정** (마이페이지 쪽 새 페이지가 그 역할을 전담)
- **Phase 6 (마이페이지)**: 완료
- **Phase 7 (인증 화면 디자인)**: 완료. 로그인/회원가입 모두 같은 톤으로 완성 (회원가입은 실제 계정
  생성 백엔드가 없어서 제출하면 "준비 중" 토스트만 뜨고 테스트 계정 로그인으로 유도)
- **Phase 8 (Supabase/API 연동)**: 다음 차례

## 지금까지 커밋된 것 (마이페이지 v2)

- **나의 자격증 / 응시기록 탭 모두 자격증(jmCd) 하나당 카드 하나**로 표시. 카드 클릭 시
  `/mypage/records/:jmCd`(새 페이지, `MyPageLayout` 탭 없이 독립적, `BackButton` 포함)로 이동해서
  그 자격증의 준비중 플랜 + 응시기록을 한곳에 모아서 보고 관리함. 같은 자격증이 두 탭에 동시에
  나타날 수 있음(준비중인 것 + 이미 끝난 것 둘 다 있을 때).
- `/mypage/records/:jmCd`에는 참고용으로 **응시 수수료 + 시험과목**도 같이 표시함
  (`getExamFee`/`getExamSubjects` — 자격증 전체 기준이고 회차별 데이터는 아님, 지금 데이터 모델의 한계).
- **결과 입력하면 플랜은 삭제**되고 응시기록에 카드로 반영됨 (`ExamRecordFormDialog`가 lockedPlan
  모드로 기록 저장 성공 시 `removeMyPlan`도 같이 호출). `ExamRecord.planId`는 lineage 표시용으로만
  남겨두고, 화면에서 필터링 용도로는 더 안 씀.
- 공개 자격증 상세페이지(`/certificates/:jmCd`)에는 "관련 응시기록" 섹션을 넣었다가 제거함 —
  역할이 `/mypage/records/:jmCd`로 완전히 이전됨.
- 공용 `ExamRecordFormDialog` (추가/수정/플랜에서 결과입력 3모드, `presetJmCd`로 특정 자격증
  미리 선택 가능)
- 자격증 선택 Combobox(검색 가능, 이름 표시), 단계 Select 라벨 표시 수정
- 관심분야/응시지역 설정, 관심 자격증 목록

## 추가 반영 (2026-09-09)

- 홈 "나의 시험" 카드, 주간 시험일정 캘린더의 접수 막대 모두 클릭 가능하게 링크 처리
  (각각 `/mypage/records/:jmCd`, `/certificates/:jmCd`)
- 홈 "관심 직무분야 자격증": 4개로 자르지 않고 해당 분야 전체를 가져와서 가로 스와이프 행으로
  표시(기본 노출은 4개, 모바일은 2개)
- 마이페이지 설정에 "개인정보" 섹션 추가 — 이름만 수정 가능(이메일은 테스트 계정 로그인 검증에
  쓰여서 읽기 전용으로 둠). `useAuth().updateName()` 추가
- 나의 자격증/응시기록 그룹 카드에 "대표 항목 1건(필/실기·연도회차·D-day 또는 합격여부) + 외 N건"
  표시, 대표 항목 삭제 버튼 추가 (나의 자격증은 결과입력 필요한 것 우선, 없으면 가장 가까운 D-day;
  응시기록은 가장 최근 응시일 기준)
- `/mypage/records/:jmCd`에 "준비 중" 섹션 위쪽에 필기/실기 각각 가장 가까운 예정 회차를 보여주고
  바로 추가할 수 있는 위젯 추가 (기존 `getExamSchedules` + `AddMyPlanButton` 재사용, 이미 추가된
  회차는 자동으로 안 보임). `AddMyPlanButton`에 `onChange` 콜백 추가해서 이 페이지가 자기 `plans`
  상태를 새로고침할 수 있게 함

## 추가 반영 (2026-09-09, 2차)

- 나의 자격증/응시기록/`AddMyPlanButton`의 삭제 핸들러들이 "화면 먼저 갱신 → 실제 저장 나중"
  순서라 경쟁 상태(race condition)가 있었음 (삭제 직후 위젯이 새로 마운트되며 아직 안 지워진
  localStorage를 읽어서 "추가됨"으로 잘못 표시됨) → 전부 "저장 먼저 → 화면 갱신"으로 순서 수정
- 응시기록 그룹 카드에 대표 기록의 점수/메모도 표시 (나의 자격증 쪽은 아직 결과가 없어서 해당 없음)
- 헤더 아이콘 순서: 검색이 가장 오른쪽으로
- **색상 시스템**: `Button`의 default/outline/ghost/link variant를 "평소엔 회색, 호버하면
  브랜드 블루"로 통일 (`src/components/ui/button.tsx`), 헤더 로고는 상시 브랜드 컬러,
  카드형 텍스트 링크("자세히 보기 →" 등)도 호버 시 브랜드 컬러로
- 헤더 마이페이지 아이콘 클릭 시 드롭다운 메뉴로 변경: 마이페이지 + (비로그인 시 로그인·회원가입 /
  로그인 시 로그아웃)
- 회원가입 페이지(`SignupPage`) 신규 제작 — 로그인 페이지와 동일한 톤

## 추가 반영 (2026-09-09, 3차)

- **버튼/토글 스타일 정리**: 상태필터·관심분야 다중선택·합격/불합격 선택이 각자 따로 만든
  className을 쓰고 있던 걸 공용 `ToggleChip` 컴포넌트(`src/components/common/ToggleChip.tsx`)로
  통합. 단, 검색페이지 계열 브라우저(`CategoryBrowser`)는 목록형 선택 UI라 성격이 달라서 그대로 둠.
  "응시 기록 추가" 버튼이 페이지마다 variant가 다르던 것도 `outline`으로 통일.
- **빈 상태 카드 패딩 확대**: 나의 시험/관심 직무분야/나의 자격증/응시기록/관심자격증/시험일정
  등 데이터 없을 때 뜨는 카드 전부 `py-10`으로 여유 있게.
- **월별 시험 일정 페이지(`/schedule`) 신규 추가**: 진짜 달력 그리드, 오늘 강조, 접수기간(파랑
  점)/시험일(검정 점) 구분 표시, 날짜 클릭 시 그날 일정 리스트, 월 이동. `listScheduleEventsInRange`
  서비스 함수 신규 추가(기존 주간 위젯용 함수는 그대로 둠). 헤더에 캘린더 아이콘으로 진입 가능.
- **버그 수정**: 검색페이지 `CategoryBrowser`에서 직무분야를 바꾸면 분류는 초기화되는데 시행종목
  목록(자격증 배열)이 안 지워지고 이전 목록이 그대로 남아있던 버그 수정 — 분류가 선택되어 있을
  때만 목록을 렌더링하도록 조건 추가.
- **Base UI 경고 수정**: `Button`에 `render={<Link/>}`를 쓰는 곳(로그인 유도 배너, 빈 상태
  CTA들)마다 `nativeButton={false}` 추가 — Link는 `<a>`로 렌더링되는데 Button은 기본적으로
  `<button>`이 될 거라 가정해서 경고가 떴던 것.
- **스와이프 영역을 shadcn `carousel`(embla-carousel-react)로 교체**: 기존엔 `overflow-x-auto` +
  스크롤 스냅만 쓴 순수 CSS 방식이라 터치/트랙패드는 되는데 **마우스 클릭+드래그는 원래 지원 안 되는
  기능**이었음(브라우저 기본 스크롤 동작의 한계). 나의 시험/관심 직무분야/상태필터 세 곳 다
  `Carousel`/`CarouselContent`/`CarouselItem`으로 교체해서 마우스 드래그·관성·클릭-드래그 구분까지
  전부 자연스럽게 해결됨. 나의 시험의 점(dot) 표시는 embla `select` 이벤트로 동기화하도록 변경.
- **나의 시험(홈) 정렬 로직 변경**: 시험일이 이미 지난 플랜은 목록에서 제외하고, 남은 것만 날짜
  빠른 순으로 정렬 (마이페이지의 "나의 자격증"은 결과입력 유도 목적으로 지난 시험도 계속 보여줌 —
  의도적으로 다름).
- **`/mypage/records/:jmCd`에서 응시수수료/시험과목 섹션 제거**하고 "시험 상세정보 보기" 버튼으로
  대체(원래 응시수수료가 있던 자리, `/certificates/:jmCd`로 연결) — 공개 상세페이지와 정보가
  중복되고, 회차별 데이터가 아니라 자격증 전체 기준이라 애매했던 문제를 자연스럽게 해소.
  자격증 상세페이지의 시험과목 목록도 모바일에서 과목명/문항정보가 세로로 쌓이게 수정.

## 추가 반영 (2026-09-10)

- 사용자가 정리해 온 8가지 개선사항을 조언 후 전부 승인받아 한 번에 반영:
  1. `/schedule` 캘린더를 감싸던 `Card` 외곽선 제거(공간 더 꽉 채움)
  2. 모바일에서 캘린더 날짜 칸 높이를 줄이고(`min-h-11`→`sm:min-h-16`), 날짜 클릭 시 하단 일정
     목록으로 자동 스크롤(`scrollIntoView`)되도록 추가 — 세로로 너무 길어서 캘린더만 보이던 문제 완화
  3. 검색/관심자격증에 쓰이는 `CertificateCard`에 필기/실기 각각 가장 가까운 예정 시험일을 두 줄로
     추가(없으면 "예정된 시험 없음" 안내), 기존 뱃지(자격구분/접수상태/D-day)는 그대로 유지하고
     카드 높이만 `h-44`→`h-56`으로 확장. `getNearestExamDatesByStage`(`src/lib/examStatus.ts`) 신규
     추가, D-day 텍스트 포맷은 `DdayBadge`와 겹치던 로직을 `formatDday()`(`src/lib/date.ts`)로 통합
  4. 헤더의 "{이름}님" 텍스트 제거
  5. 마이페이지 "나의 자격증"에서 눌러도 반응 없던 "결과 입력 필요" 뱃지를 실제 클릭 가능한
     "결과 입력" 버튼으로 교체(`ExamRecordFormDialog`를 `/mypage/records/:jmCd`와 동일한 패턴으로
     연결)
  6+8. 비로그인 홈에서 로그인 필요한 "나의 시험"/"관심 직무분야 자격증" 섹션이 이탈 요인이 될 수
     있다고 판단 → 비로그인 시에는 두 섹션을 감추고 그 자리에 슬로건 히어로 섹션(`HeroSection`)을
     배치. 슬로건 "자격증을 찾고, 시험을 관리하고, 다음을 준비하다"를 그대로 헤드라인으로 쓰고,
     슬로건의 세 동사에 맞춘 찾고/관리하고/준비하다 3개 아이콘 카드 + 로그인/회원가입 CTA로 구성.
     로그인 시에는 기존 그대로(나의 시험 + 관심 직무분야 + 주간 시험일정). `MyExamHero`/
     `InterestFieldCertificates` 내부의 비로그인 분기(`LoginPromptBanner`)는 이제 상위(`HomePage`)에서
     로그인 여부로 갈라주므로 제거하고, 안 쓰게 된 `LoginPromptBanner.tsx` 파일 삭제
  7. 로그인 홈 "나의 시험" 섹션 제목 옆에 "+ 시험 추가" 버튼 신규 추가 → 모달(`QuickAddPlanDialog`)에서
     자격증 검색(Combobox) → 단계 선택 → 실제 회차 선택(없으면 직접입력)까지 바로 처리해서 검색→
     상세페이지→등록의 기존 3단계를 홈에서 한 번에 끝낼 수 있게 함. 기존 `ExamRecordFormDialog`의
     회차 선택 패턴을 그대로 재사용하되 합격여부/점수/메모 없이 플랜 생성(`addMyPlan`)만 하도록 축소
- `npx tsc -b --noEmit`, `npx oxlint`, `npm run build` 모두 확인 완료(빌드 성공, 기존에도 있던
  워닝 몇 개만 남음 — 새 코드에서 발생한 에러는 없음)

## 추가 반영 (2026-09-10, 2차)

- **`CertificateCard` 레이아웃 반복 수정**: 필/실기 예정 시험일을 D-day 표기가 아니라 실제 날짜
  (`YYYY.MM.DD`)로 바꾸고 위치를 자격증명↔태그 사이로 이동, 국가기술자격 등 자격구분 라벨은
  뱃지가 아니라 자격증명 바로 위 옅은 텍스트로 변경. 카드 세로 사이즈가 너무 커졌던 문제는
  `Card size="sm"`/고정 높이(`h-56`)를 걷어내고 내용 기반 자연스러운 높이로 되돌리는 것으로 해결.
  홈 "관심 직무분야 자격증"과 자격증 상세페이지 "유사 분야 자격증" 두 곳 모두 기존 심플 카드
  (`CertificatePreviewCard`, 이번에 삭제) 대신 이 `CertificateCard`를 그대로 재사용하도록 통일.
  "유사 분야 자격증"은 3개로 자르지 않고 전체(`getSimilarCertificates(jmCd, Infinity)`)를 가져와서
  캐러셀로 스와이프하되 기본 노출은 데스크탑 3개(`sm:basis-1/3`)/모바일 2개로 조정.
- **나의 자격증 / 응시기록 리스트를 "대표 1건 + 외 N건" 대신 그룹당 최대 2건 나열**로 변경, 응시기록은
  각 건마다 수정(`ExamRecordFormDialog` edit 모드)·삭제 버튼을 바로 붙여서 상세페이지에 안 들어가고도
  즉시 처리 가능하게 함. 나의 자격증(플랜)은 애초에 수정 기능 자체가 없어서 결과입력/삭제만 유지.
- **응시기록 점수 입력에 100점 상한 검증 추가**: `Input`에 `max=100` 지정 + 100점 초과 시 입력란
  아래 빨간 문구 + 저장 시 토스트로 막음 (`ExamRecordFormDialog.tsx`).
- **디자이너가 만든 스타일 가이드(`styleguide.png`) 이미지를 픽셀 단위로 직접 스포이드 추출해서
  반영**: 이미지에 적힌 hex 텍스트와 실제 스와치 색이 서로 다른 경우(Primary 색상)가 있어서
  임의 판단하지 않고 사용자에게 확인 후 진행.
  - 새 디자인 토큰 추가(`src/index.css` `@theme`): `--color-brand`(`#2563eb`→`#0075d2`로 교체),
    `--color-brand-light`, `--color-neutral`, `--color-neutral-light`, `--color-status-green(-bg)`,
    `--color-status-orange(-bg)`, `--color-status-red`, `--color-line`(#D9D9D9, outline 버튼 공용 테두리)
  - `StatusBadge`(접수예정=주황/접수중=초록/접수마감=회색), `DdayBadge`(진한 블루 필 → 연한
    브랜드라이트 배경+블루 텍스트로 톤 다운 — D-day가 상태뱃지보다 튀어서 시선이 분산된다는
    피드백 반영), 응시기록 합격/불합격 뱃지 전부 가이드 색상으로 교체
  - `Button`의 `outline` variant 기본 테두리를 `border-border`→`border-line`으로 바꿔서 outline
    버튼 전체(수정/삭제/기록추가/시험추가/시험상세정보/전체일정보기)에 일괄 적용, "추가" 계열
    버튼도 테두리는 회색으로 통일하고 텍스트만 브랜드블루로 구분
  - "시험 상세 정보"(문구도 "시험 상세정보 보기"→"시험 상세 정보"로 변경)/"전체 일정 보기" 두
    버튼을 텍스트 링크에서 실제 버튼(↗ 아이콘 포함)으로 승격
  - 합격/불합격 뱃지와 수정/삭제 버튼의 높이·좌우패딩을 `h-7`/`px-3`로 통일(원래 `Badge` 기본
    높이가 `Button`보다 작아서 나란히 두면 크기가 안 맞았음)
- **검색 페이지 직무분야 컬럼에 "전체" 항목 추가**: 한번 직무분야를 선택하면 새로고침 전엔 초기
  상태로 못 돌아가던 문제 해결 (`CategoryBrowser`에 `onClearJobField` 콜백 추가)
- **헤더 아이콘 순서**: 검색 → 달력 → 마이페이지 순으로 재배치

## 추가 반영 (2026-09-11)

- **다른 컴퓨터(홈)에서 `node_modules` 동기화 문제 발견/해결**: 학원에서 shadcn/ui 등 패키지를
  추가했는데, 다른 컴퓨터의 `node_modules`는 그 이전 상태로 멈춰있어서 `next-themes` import 실패
  (`[plugin:vite:import-analysis] Failed to resolve import`, 콘솔에 500 에러)가 발생함 →
  `npm install`로 해결. **컴퓨터를 오갈 때 `git pull` 이후 `package.json`이 바뀌어 있으면
  꼭 `npm install`도 같이 돌릴 것** (안 그러면 이 에러가 반복됨).
- **비로그인 홈 히어로 섹션(`HeroSection.tsx`) 리디자인**: `레퍼런스 이미지/` 폴더에 넣어둔 두
  사이트 스크린샷(STAYERA, DseForge)을 실제로 참고해서 진행.
  - CTA 버튼(로그인/회원가입) 제거
  - 헤드라인을 시적인 슬로건("자격증을 찾고, 시험을 관리하고, 다음을 준비하다")에서 기능을 직접
    서술하는 문구("자격증 검색부터 시험 일정 관리, 응시 기록까지 한 곳에서")로 교체 — "처음 보는
    사람도 이 섹션만 보고 앱이 뭘 하는 곳인지 알 수 있어야 한다"는 요구사항 반영
  - 기존 3칸 카드(테두리+아이콘+제목+설명 문장)를 **화살표로 연결된 아이콘 흐름**으로 교체
    (자격증 검색 → 시험 일정 등록 → 응시 기록 관리, `bg-brand-light` 원형 배지 아이콘 +
    `ChevronRight` 커넥터, 설명 문장 없이 라벨만). STAYERA의 "STEP01→04 화살표 흐름" 섹션에서
    착안 — 화살표 연결 자체가 "순서가 있는 사용 흐름"이라는 걸 시각적으로 전달해서, 정적인 기능
    나열형 카드보다 "어떻게 쓰는지"가 한눈에 읽히는 효과를 노림
  - DseForge 히어로 옆의 체크리스트 미리보기 카드("패키지 판단에 필요한 네 가지") 패턴도 검토
    했으나, 헤드라인을 이미 구체적으로 바꾸면서 같은 내용을 카드로 한 번 더 반복하게 되는
    문제가 있어 **채택하지 않음**(길이도 늘어나서 "짧게" 요구사항과도 배치됨)
  - "QUALI 소개" 배지는 제거, "이용 방법" 배지는 유지
  - 섹션 패딩/간격 확대(`py-12`→`py-16 sm:py-24`, `gap-8`→`gap-12`, 아이콘 배지 `size-11`→
    `size-12 sm:size-14`)로 "빡빡하다"는 피드백 반영해 여백감 개선
- **마이페이지 UI 정리**: "데이터는 왼쪽 정보 블록에, 오른쪽 버튼 영역은 액션만" 원칙으로 통일
  - `MyCertificatesPage.tsx`: 오른쪽 버튼 영역에 있던 `DdayBadge`가 삭제 버튼과 나란히 있어서
    "버튼처럼 보인다"는 문제 제기 → D-day를 제거하고 왼쪽 날짜 텍스트에 병합
    (`2026.03.15 · D-12`), 오른쪽엔 액션 버튼(결과입력/삭제)만 남김. (자격증 이름 옆으로 옮기는
    안도 검토했으나, 카드 제목은 그룹 전체에 하나뿐이고 D-day는 회차(row)마다 다른 값이라 구조상
    안 맞아서 채택하지 않음)
  - `RecordsPage.tsx`: 같은 이유로 합격/불합격 `Badge`를 제거하고, 왼쪽 날짜·점수 텍스트 줄에
    색깔 텍스트로 병합(`82점 · 합격`은 브랜드블루, `불합격`은 뉴트럴 그레이) — 마이페이지 두
    목록의 디자인 원칙을 통일
  - `InterestFieldCertificates.tsx`: 모바일(390px)에서 카드가 정확히 2개씩 노출되며 카드 레이아웃이
    찌그러지는 문제 → `CarouselItem`의 `basis-1/2`를 `basis-2/3`로 바꿔 1.5개 노출로 수정
  - 접수마감 `StatusBadge` 배경색(`--color-neutral-light`)이 옆의 접수예정/접수중 파스텔 배경보다
    무채색이라 상대적으로 진하게 느껴지는 문제 → `#e5e7e9` → `#eef0f2`로 더 밝게 조정
- `npx tsc -b --noEmit`, `npx oxlint`, `npm run build` 매 변경마다 확인 완료 (신규 에러 없음)

## 추가 반영 (2026-09-11, 3차)

- **검색 페이지 버그 수정**: `CategoryBrowser`에서 "전체"를 눌러 직무분야를 해제해도 이전에 골랐던
  분류(중간 컬럼) 목록이 지워지지 않고 남아있던 버그 수정 — 시행종목 컬럼에는 이미 있던
  `selectedJobFieldCode &&` 조건이 분류 컬럼에는 빠져있었음.
- **검색 페이지 모바일 UX**: 직무분야/분류/시행종목 3컬럼 브라우저가 모바일에서 스크롤 가능한
  패널 3개가 쌓이며 화면을 다 차지하는 문제 → 모바일(`sm:hidden`)에서는 3개의 `Select` 드롭다운으로,
  데스크탑(`sm:` 이상)은 기존 3컬럼 그대로 유지하는 반응형 분기 적용. 이전 단계 미선택 시 다음
  드롭다운은 비활성화 + 안내 문구 표시로 기존 흐름 유지.
- **홈 화면 모바일 반응형 손질**:
  - 히어로 헤드라인이 390px에서 강제 줄바꿈(`<br/>`) 때문에 3줄로 어색하게 깨지던 문제 →
    모바일 기준 폰트를 낮추고(`heading-1` 유틸리티 자체에 반응형 내장) `<br/>`은 `sm:` 이상에서만
    적용되게 수정
  - "이용 방법" 3단계에 캡션(직무에 맞게 찾아요 등) 추가했다가 라벨까지 줄바꿈되며 깨지는 문제 발생
    → 라벨은 `whitespace-nowrap`으로 한 줄 고정, 캡션만 `max-w-20`으로 폭 제한해 2줄까지 허용
  - **시험 일정 위젯(`UpcomingExamScheduleWeek`) 재설계**: 접수기간을 막대로 겹쳐 그리던 방식이
    좁은 화면에서 텍스트가 심하게 잘리는 문제 → `/schedule` 페이지처럼 날짜 밑에 점 찍고 그 아래
    전체 리스트를 나열하는 방식으로 통일(데스크탑도 동일). 이후 사용자가 "월 표시가 사라졌다",
    "날짜 칸 테두리가 없어졌다", "날짜가 리스트에 중복으로 나온다"고 피드백 → 월 라벨/칸 테두리
    복원, 리스트는 "날짜" 대신 "색 점 + 일정 이름"만 남기는 것으로 재조정. 날짜 범위도 "오늘부터
    7일"에서 "일~토 캘린더 주" 기준으로 변경. 마지막으로 이 위젯이 접수기간만 보여주고 시험일은
    안 보여줘서 `/schedule`과 내용이 다르다는 지적을 받아, `listRegistrationWindowsInRange` 대신
    `listScheduleEventsInRange`로 교체해 접수기간(파랑 점)+시험일(검정 점) 둘 다 표시하도록 통일
    (안 쓰게 된 `listRegistrationWindowsInRange`/`RegistrationWindowEntry`는 삭제)
  - `/schedule` 월별 캘린더의 날짜 칸이 1024px 이상 화면에서 가로만 넓어지고 세로는 그대로라
    납작해 보이던 문제 → 고정 높이 대신 `aspect-square`로 바꿔 칸 너비에 맞춰 항상 정사각형 유지
- **스타일 가이드(디자이너 전달) 2차 반영**: 사용자가 실측한 값 기준으로 라운드 10px(캡슐형 X),
  outline 버튼 공통 테두리 `#D9D9D9`, 합격/불합격·수정/삭제 패딩 통일, D-day 톤 다운(진한 채움 →
  연한 배경) 등 세부 수치 교정. 전역 `--border`/`--input` 토큰을 `--color-line`(#D9D9D9) 참조로
  통합해서 버튼별 예외 처리 없이 앱 전체가 한 회색을 쓰도록 정리.
- **검색/관심분야/유사분야 카드(`CertificateCard`)의 D-day 위치 재정리**: 필/실기 중 어느 시험의
  D-day인지 애매하게 별도 뱃지로 떠 있던 걸, 각 단계 줄 자체에 `2026.03.15 · D-12`처럼 병합. 마이페이지
  쪽(나의 자격증/자격증별 상세)도 동일 원칙으로 통일하면서 그 김에 오래돼서 디자인이 안 맞던
  `MyCertificateRecordPage.tsx`(수정/삭제 ghost 버튼, 예전 색 뱃지)도 최신 디자인으로 재작업.
- **타이포그래피 스케일(H1~H5 제목 / D1~D5 설명) 전면 도입**: 사용자가 만든 실제 수치(H1 52px 등)를
  검토해서 모바일 대응·계단 간격 등을 조언 후 합의된 값으로 `index.css`에 `@utility heading-1~5`,
  `desc-1~5` 정의(H: 44/36/30/24/20px 데스크탑, 전부 bold; D: 24/20/16/14/12px, 전부 medium — 이
  값이 우연히 Tailwind 기본 `text-2xl~text-xs`와 정확히 일치해서 앱에서 제일 많이 쓰이던 14px/12px
  텍스트가 자동으로 D4/D5에 편입됨). 사이트 전체 제목·본문 설명·빈 상태 문구·카드 메타정보 등
  40곳 이상에 적용.
- **준비물 체크리스트 기능 신규 구현** (전에 구현 방법을 못 정해 보류했던 것을 이번에 완성): 홈 "나의 시험" 카드가 시험
  D-1/D-day일 때 카드 하단에 신분증/수험표/필기구 체크박스 + 사용자 커스텀 항목을 보여줌.
  `ExamChecklist` 타입, `userService.ts`에 저장/토글/추가/삭제 함수 추가(플랜 삭제 시 체크리스트도
  같이 정리), shadcn `Checkbox` 컴포넌트 신규 추가(체크 색은 브랜드 블루로 커스텀).
- **오늘 마지막 배치**:
  - 마이페이지 설정의 "응시 지역" 섹션 완전 삭제 — 코드 추적 결과 이 값을 실제로 읽어서 쓰는
    곳이 앱 어디에도 없어서(저장만 되고 아무 기능에도 영향 없음) 사용자와 상의 후 제거로 결정.
    `UserSettings.examRegionCodes`, 관련 토글 함수, `mocks/branches.ts` 전부 삭제
  - "관심 분야" 섹션에 여러 개 선택 가능하다는 안내 문구 한 줄 추가
  - D-day 색이 카드마다 제각각이던 것(검색 카드는 브랜드블루, 홈 히어로 큰 숫자는 무채색, 마이페이지
    목록은 회색)을 전부 브랜드 블루로 통일
  - 홈 "관심 직무분야 자격증" 모바일 캐러셀이 1.5개 노출(`basis-2/3`)이라 가로폭이 애매하다는
    피드백 → 1.3개 노출(`basis-[76.9%]`)로 조정
  - 준비물 체크리스트의 입력창+추가 버튼이 항상 노출돼 있어 무거워 보이던 것 → "+" 아이콘 버튼만
    남기고 누르면 다이얼로그가 열려서 입력 + "등록" 버튼이 뜨는 구조로 변경, 등록 시 토스트 알림
- `npx tsc -b --noEmit`, `npx oxlint`, `npm run build` 매 변경마다 확인 완료 (신규 에러 없음)

## 남은 것

- 마이페이지 전체적으로 브라우저에서 실사용 흐름 검증 필요 (플랜 추가 → 결과 입력 → 그룹 카드
  반영 → 새 페이지 이동까지 한 사이클을 직접 클릭해보면서 확인하면 좋음)
- `Footer.tsx`/`MyPageLayout.tsx`에 브랜드 컬러 도입 이전의 `border-gray-200`/`text-black`/
  `text-gray-500` 하드코딩이 아직 남아있음 (발견만 해두고 아직 안 고침)
- `getTestSites`/`mockTestSites`(시험장 후보 목록 mock) — 시험장 정보 기능 자체를 폐기하면서
  이 데이터를 쓰는 화면이 없어짐. 완전히 안 쓰는 코드라 정리 대상 후보
- 다음은 Phase 8: Supabase Edge Function + pg_cron으로 Q-net API 연동 시작

## 테스트 계정

- 이메일: `test@quali.com` / 비밀번호: `1234` (`src/lib/auth.tsx`의 `TEST_ACCOUNT`)
- 로그인하면 `seedDemoDataIfNeeded()`가 최초 1회 나의 시험/관심분야 데모 데이터를 채워줌

## 개발 명령어

```
npm run dev      # 개발 서버 (사용자가 직접 실행 — Claude가 임의로 띄우지 않음)
npm run build    # tsc + vite build
npx tsc -b --noEmit   # 타입체크만
npx oxlint       # 린트
```

다른 컴퓨터에서 `git pull` 했는데 `package.json`이 바뀌어 있으면 위 명령어 실행 전에
`npm install`부터 돌릴 것 (안 그러면 새로 추가된 패키지 import가 실패함).

## 참고

- `.env`의 `QNET_API_KEY`로 Q-net API 직접 조회 가능 (실제 데이터 확인용, 아직 앱에서 직접 호출은 안 함)
- `scripts/test-qnet-api.mjs`, `scripts/fetch-qnet-snapshot.mjs`로 서버 사이드 호출 + XML→JSON
  파싱이 실제로 되는지 검증해둠 (`node --env-file=.env scripts/fetch-qnet-snapshot.mjs`). 결과는
  `data/qnet/`에 mock 19개 자격증 전부 저장되어 있음 — Edge Function 만들 때 참고자료로 재사용 가능.
  `parseTagValue: false` 꼭 필요(안 하면 `jmcd="0080"` 같은 코드값 앞자리 0이 날아감).
- 카드 컴포넌트 등은 shadcn CLI로 관리 (`npx shadcn@latest add <component>`)
- 커밋 메시지는 "왜"를 설명하는 스타일 유지, Co-Authored-By 트레일러 포함
